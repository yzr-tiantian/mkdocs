# 6-路径
### 目录文件

目录文件：目录文件是文件夹，文件夹中可以包含文件和子文件夹。

### 根目录

根目录：打开目录文件夹的第一层就是根目录。

### 相对路径

相对路径：相对路径是相对于当前文件的路径。

### 绝对路径

绝对路径：绝对路径是完整的路径，从根目录开始，到当前文件路径结束。

### 正反斜杠

正反斜杠：在Windows系统中，目录路径使用反斜杠（\\），而在Linux系统中，目录路径使用正斜杠（/）。