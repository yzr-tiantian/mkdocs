# 9-布局标签
### 网站结构标签

网页的外观多种多样，但是大概都包含：

*   页眉、导航栏、主内容、侧边栏、页脚等

| 标签  | 作用  |
| --- | --- |
| `<header>` | 网页页眉(头部) |
| `<main>` | 网页内容。每个页面只能有一个 |
| `<nav>` | 导航栏 |
| `<article>` | 文章相关 |
| `<section>` | 分块  |
| `<aside>` | 侧边栏 |
| `<footer>` | 页面页脚(底部) |

![alt text](9-%E5%B8%83%E5%B1%80%E6%A0%87%E7%AD%BE.assets/image.png)

* * *

### 无语义标签

没有适语义标签时，在进行一些布局时候可以选择：div和 span 标签

| 标签  | 作用  |
| --- | --- |
| `<div>` | 块级元素，没有特殊含义，用于布局 |
| `<span>` | 行内元素，没有特殊含义，用于布局 |

* * *

### 列表标签

HTML列表是网页内容组织的重要元素。可以让我们显示内容更加整齐有序

| 标签  | 作用  |
| --- | --- |
| `<ul>` | 无序列表，列表项前默认显示圆点 |
| `<ol>` | 有序列表，列表项前默认显示数字 |
| `<li>` | 列表项，必须放在ul或ol中 |
| `<dl>` | 自定义列表，包含多个术语和描述 |
| `<dt>` | 自定义列表中的项目 |
| `<dd>` | 自定义列表中的描述 |